untuk foreach <br>
foreach ($data["nilai_key"] as $k => $value) // itu enaknya buat tipe array yang ada key serta value <br>
foreach ($data["nilai_key"] as $k) // itu enaknya buat tipe array yang single key
